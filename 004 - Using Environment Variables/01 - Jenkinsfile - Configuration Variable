pipeline {
	agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
	environment { 
        NOTIFICATION_EMAIL = 'example@example.org'
    }
	stages {
		stage('build') {
			steps {
				echo "$NOTIFICATION_EMAIL"
			}
		}
	}
}
