pipeline {
	agent none
    stages {
		stage('maven stage') {
			agent { docker 'maven:3-alpine' } 
			steps {
				sh 'mvn --version'
			}
		}
		stage('maven stage') {
			agent { docker 'node:6.3' } 
			steps {
				sh 'npm --version'
			}
		}
	}
}
