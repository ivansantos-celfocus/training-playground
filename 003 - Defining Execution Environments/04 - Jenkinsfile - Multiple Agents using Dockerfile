pipeline {
	agent none
	stages {
		stage('maven stage') {
			agent { 
				dockerfile {
					filename '04 - Jenkinsfile - Maven Dockerfile'
				}
			} 
			steps {
				sh 'mvn --version'
			}
		}
		stage('node stage') {
			agent {
				dockerfile {
					filename '04 - Jenkinsfile - Node Dockerfile'
				}
			} 
			steps {
				sh 'npm --version'
			}
		}
	}
}
